# 測試腳本(用戶端 × 商家端 × 雙端聯動 × 小程序貨架)

> 給人照著點的逐步表。每步都寫「預期看到什麼」——看不到就是 bug,記步號回報。
> 改完商家端四檔任一個,重跑 `python checks/verify-demo.py`(exit 0 才算完成)。
> 改完 miniapps 任一支,重跑 §2.5-6 的三條指令。

**只有 15 分鐘、要挑一段測**:§2.5-2(解毒吧主動線,含回訪)。它是目前唯一有真資料鏈路的一支。

## 0. 開場前設定

**線上(推薦,手機可用)**

| 用途 | 網址 |
|---|---|
| 商家工作台(4 分頁殼) | https://robertshao-cmd.github.io/fapiao-demo/merchant.html |
| 用戶端小程序(暖暖窩) | https://robertshao-cmd.github.io/fapiao-demo/user.html?store=nuan |
| 載具首頁(真 App 重建) | https://robertshao-cmd.github.io/fapiao-demo/real-home-v7.html |
| **小程序貨架**(3 顆單元) | https://robertshao-cmd.github.io/fapiao-demo/mini/ |

Pages 的 HTML cache 是 600 秒。剛 push 完要測 → 網址後面加 `?v=<隨便數字>`,或手機長按重新整理。

**本機**

```bash
python -m http.server 8125 --directory .
```

→ `http://localhost:8125/merchant.html`;手機同網段用 `http://192.168.18.22:8125/merchant.html`
(IP 每次開機可能變,用 `ipconfig` 重抓)。

**唯一硬限制**:雙端聯動走 BroadcastChannel,**同源才通**。`file://` 直開會正常渲染但聯動全部靜默失效。
兩種可行擺法:

- **A 單視窗**(適合一支手機給老闆看):只開 `merchant.html`,靠內建 4 分頁切換。iframe 同源,聯動一樣通。
- **B 雙視窗**(適合桌機演「即時彈進來」):同一個 origin 開兩個 tab —— tab1 `merchant.html`、tab2 `user.html?store=nuan`。

---

## 1. 商家端(biz)

### 1-1 登入殼

| 步 | 操作 | 預期 |
|---|---|---|
| 1 | 開 `merchant.html` | 登入頁:暖暖窩義式廚房 · 統編 **24567890** · 密碼已預填 |
| 2 | 按「一鍵登入(示範帳號已預填)」 | 進工作台,topbar 顯示同一個統編 24567890,底部 4 分頁 |
| 3 | 逐一點底部 4 分頁 | 商家助手 / 生意參謀 / 用戶端預覽 / 載具首頁,每個都載出畫面(不是白畫面) |
| 4 | 右上「⋯」→ ↻重新整理 | toast 顯示「已重新整理『<當前分頁名>』」——名字要跟你在看的那頁一致 |

### 1-2 商家助手 → 🏗️ 開店(5 步,含開店計時)

| 步 | 操作 | 預期 |
|---|---|---|
| 5 | 「🔎 以統編自動帶入登記資料」 | 帶入「暖暖窩義式廚房(板橋店)」+ 發票設定 OK |
| 6 | 下一步:拍菜單 → 「📷 一鍵 AI 辨識菜單」 | 菜單品項自動長出來 |
| 7 | 確認菜單 → 營運與支付,切「接/不接第三方支付」 | 開關可切;**這個開關會同步到用戶端**(見 3-6) |
| 8 | 下一步:選風格 → 「🚀 一鍵上線」 | 上線頁 + 開店計時顯示總耗時 |

### 1-3 🧾 訂單(接單 + 核銷台)

| 步 | 操作 | 預期 |
|---|---|---|
| 9 | 「▶ 離線示範:模擬一張用戶預點單」 | 看板訂單數 +1 |
| 10 | 「接單 · 開始製作」 | 卡片轉製作中,計時器開始跑 |
| 11 | 「出餐完成」 | 卡片轉「已完成 · 已叫號」 |
| 12 | 核銷台(頁面上方金框) | 沒券時顯示空狀態;有券時見 3-4 |

### 1-4 📊 數據 / 👥 會員 / 🚀 增長

| 步 | 操作 | 預期 |
|---|---|---|
| 13 | 📊 數據 | 今日日期貼近當下;30 天回購 **34%**、商圈義式平均 **27%**、回頭客 **59%**/新客 **41%**(圈圖中央寫 59%) |
| 14 | 👥 會員 → 4 個分層各按「發券」 | 按鈕轉「已完成 ✓」,對應人數/文案正確(熟客/一般 792/沉睡 182/流失風險) |
| 15 | 🚀 增長 → 「一鍵上架」+「發送喚回券(即時聯動)」 | 兩顆都轉已完成;喚回券會**射到用戶端券夾**(見 3-3) |
| 16 | 增長頁「解鎖 · NT$1,000/月」 | toast 說明進階訂閱是選配(不真的解鎖) |

### 1-5 生意參謀(第 2 分頁)

| 步 | 操作 | 預期 |
|---|---|---|
| 17 | 切 5 個分頁:📊總覽 / 🏙️行業 / 👥客戶 / 🥊競對 / 🆚對比 | 每頁都有內容 |
| 18 | 找對標語言 | 一律寫「**商圈前 25% 聚合均值(去識別化)**」——出現「標杆店」= bug(verify 會擋) |
| 19 | 客戶頁 RFM 4 顆發券鈕 | 都轉已完成 |
| 20 | 數字對照第 13 步 | 34% / 27% / 59% / 41% 兩邊必須一樣 |
| 21 | 開獎日文案 | 下一個開獎日 = **9/25(五)**(單數月 25 日,不得已過期) |

---

## 2. 用戶端(user)

用 `user.html?store=nuan`,或殼的第 3 分頁「用戶端預覽」。

| 步 | 操作 | 預期 |
|---|---|---|
| 22 | 底部 6 格:🏠首頁 / 🎁活動 / 👛載具管理 / ▣掃描對獎 / 🧾我的發票 / 👤個人 | 首頁、活動有真頁面;其餘 3 個是示範 toast(刻意) |
| 23 | 首頁點「暖暖窩」 | 品牌開屏動畫 → 夜色氛圍整版接管 |
| 24 | 品牌頁「我的折扣券」 | 底部浮層列出該店券;**新客見面禮只有一列**(領完不重複) |
| 25 | 浮層「領取」(見面禮) | 該列消失、變成清單裡的一張券;不再出現領取鈕 |
| 26 | 券上「到店出示」 | toast「已出示…請店員核銷」→ **商家端核銷台亮**(見 3-4) |
| 27 | 回首頁 → 🎁活動 → 我的券夾 | 同一批券在這裡也看得到,每張都有藥丸形「🎫 到店出示」鈕 |
| 28 | 點菜 → 結帳 → 下單 | 訂單成立;**商家端訂單看板即時 +1**(見 3-1) |
| 29 | 訂單頁「▶ 示範:模擬店家完成製作」 | 跳「請取餐」 |
| 30 | 送出評價 | CM 幣 +5 |
| 31 | 加入會員(一鍵授權入會) | 入會成功;**商家端會員 +1**(見 3-7) |
| 32 | 鼎泰豐 → 內用排隊取號 | 取號成功;商家端 toast |

### 按鈕形制(對照微信卡券)

「到店出示」= 藥丸形(radius 99px)、品牌色填滿、高 32px、按壓縮放 —— 對應微信卡券右側「去使用」主行動鈕。
量到的實測值:`圓角 99px / 高 32px / 底色 rgb(47,169,140)`。

---

## 2.5 小程序貨架(mini:解毒吧／懂你地圖／財神開示)

跟 §1–§3 完全獨立——**不走 BroadcastChannel、不需要商家端**,單開一個網址就能測完。
下面每個「預期」都是 0729 在**建置後的靜態版**上實測到的字面值,對不上就是 build 壞了。

| 用途 | 網址 |
|---|---|
| 貨架(入口殼) | https://robertshao-cmd.github.io/fapiao-demo/mini/ |
| 🧪 解毒吧 | https://robertshao-cmd.github.io/fapiao-demo/mini/detox/ |
| 🗺️ 懂你地圖 | https://robertshao-cmd.github.io/fapiao-demo/mini/bored/ |
| 🧧 財神開示 | https://robertshao-cmd.github.io/fapiao-demo/mini/wealth/ |

本機測同一份產物:`python -m http.server 8125 --directory .` → `http://localhost:8125/mini/`
(這才是「線上會長怎樣」;5180-5183 的 node server 是**開發**用,行為不完全一樣)

### 2.5-1 貨架殼

| 步 | 操作 | 預期 |
|---|---|---|
| 33 | 開 `/mini/` | 標題 **發票載具 · 官方小程序 DEMO**;4 顆磁貼:🧪解毒吧 / 🗺️懂你地圖 / 🧧財神開示 / 🍝暖暖窩點單 |
| 34 | 看來源切換鈕 | 左邊寫「**這個 demo**」(不是「本機 dev」);**沒有**「其他單元改了什麼」那張卡 |
| 35 | 點任一磁貼 → 單元開起來 → 按左上 ‹ | 回到貨架。**不可以跳到 localhost:5180**(靜態版注入 `UK.HOME='../'`) |

磁貼名字錯(例如 bored 還叫「無聊快篩」)= 來源沒改到,build 會直接擋。

### 2.5-2 解毒吧(主動線,含回訪)

| 步 | 操作 | 預期 |
|---|---|---|
| 36 | 進解毒吧 | 「你以為你吃得還行 / 你的發票不這麼認為」+ 底部「近 30 天 47 張發票 · 只算你自己**（示範數據）**」 |
| 37 | 「我敢驗」 | 先猜頁;滑桿沒動時鈕是「先拉一個數字」(不能直接送) |
| 38 | 拉滑桿到 40 | 鈕變「我猜好了,開驗!」 |
| 39 | 開驗 | 🧾 掃描過場 → 單毒揭示(「咖啡因灌進來了」)→ 結果頁,約 7 秒 |
| 40 | 看結果頁 | 綜合 **76/100**、稱號「油糖雙修有成」;五毒 = 糖 **82** / 油 **74** / 鈉 **58** / 加工 **46** / 咖啡因 **66**;兇手三名「全糖手搖 ×12 · 鹽酥雞 ×5 · 超商甜點 ×7」 |
| 41 | 點大頭像(分身)→「🧑‍🎨 曬分身」 | 分享卡浮層;canvas **1080×1350**,圖內有 QR(掃碼直達當前網址) |
| 42 | 「💊 去解毒」 | 3 家解毒站(暖暖窩 400m / 原味時代 350m / 果漾 650m);頁尾寫「只做正面認證;名單與距離為示範」 |
| 43 | 「掃載具解毒」→「✅ 店員掃好了」 | 條碼浮層寫「示範條碼 · 正式版會帶出你自己的載具號」;確認後綜合 **76 → 69**(糖 −5 · 油 −2),該卡轉「✅ 已解毒」 |
| 44 | **重新載入這一頁** | 入口變「上次實測 · 今天 **69**/100」+「看上次結果 ›」。這一步在測**回訪計數**——localStorage 的 `visits` 要變 2,**不可以歸零**(0729 修過這個 bug) |

### 2.5-3 懂你地圖

| 步 | 操作 | 預期 |
|---|---|---|
| 45 | 「看我的懂你地圖」 | 口味設定頁:同溫層 vs 沒被洗版;附「這些推薦是怎麼來的」——**必須寫**「不讀你買了什麼品項來評價你,不把你的紀錄給商家」 |
| 46 | 「看我的推薦」 | 類型統計(便利商店 13 / 咖啡 5 / 超市量販 4 / 獨立書店 3 / 展覽文創 2)+ 推薦卡「巷弄選書店」,理由引用「去了 3 次獨立書店(最近一次 6 天前)」 |
| 47 | 下一張 / 收藏 / 分享 | 卡片可換、收藏計數會動、分享出得來 |

### 2.5-4 財神開示

| 步 | 操作 | 預期 |
|---|---|---|
| 48 | 「求一支財籤 🥠」 | 籤一·守財(三選一)→ 每支籤選完按「擲筊,請財神開示」才進下一支;可「‹ 回上一支」 |
| 49 | 四支籤走完 | 結果頁:「**N 年的停工底氣**」+ 推估月支出／反推月收入 |
| 50 | 看免責字樣 | 開場與結果都要有「**娛樂性質・情境試算、非財務預測**」與「非專業財務建議」;缺任一條就是合規問題,不是文案問題 |
| 51 | 「不是這個收入?自己設定」 | 收入級距浮層(3–5 萬 / 5–8 萬 / 8 萬以上 / 用推估值) |
| 52 | 「📤 曬我的財神籤」 | 分享卡 canvas **1080×1350** |

### 2.5-5 真資料模式(**只在本機,不上線**)

`?personal=1` 會讀同目錄的 `personal.json`——那是**真實消費紀錄**,已 gitignore,
公開 demo 的 builder 是白名單複製(只帶 `index.html` + `unit-kit.js` + `qrcode.min.js`),
所以這個模式在 GitHub Pages 上**必然**讀不到檔、必然退回示範值。要給人看只能面對面。

```bash
python nest-integration/personal-poison.py /<載具號> --days 90   # 產 personal.json
# 開 http://localhost:5181/?personal=1
```

| 步 | 操作 | 預期 |
|---|---|---|
| 53 | 開 `localhost:5181/?personal=1` | toast「真資料模式:/N***** 近 90 天(M/D 產生)」;底部發票數變成真的、**「（示範數據）」字樣消失** |
| 54 | 開 devtools 看 | `UK.data.source === 'personal'`、`UK.DEMO === false`;state key 是 `detox_state_p`(跟示範值分開存,不會互相污染) |
| 55 | 把 `personal.json` 改名再開 | toast「personal.json 讀不到,沿用示範值」,頁面**照常能用**(退路,不是壞掉) |
| 56 | 不帶參數開 | 一切照示範值,跟步 36 完全一樣 |

### 2.5-6 交付閘門(改了 miniapps 任一支就跑)

```bash
cd ../miniapps && python _kit/verify.py && python _kit/flow.py --check
cd ../local-life-lab && python scripts/build-public-demo.py
```

`verify.py` 擋共用層被改壞／能力被重新實作／port 衝突／shopId 兩處不一致;
`flow.py --check` 擋 FLOW.md 跟 code 的動線脫節(**當 FAIL 不當警告**);
build 每個字串替換都有錨點斷言,來源改過而腳本沒跟上會**失敗**而不是默默產壞檔。

---

## 3. 雙端聯動(協定 9 條,`fapiao-demo` 頻道)

擺法 B(兩個 tab)最好看。每條都要**兩邊同時對**。

| # | type | 觸發 | 用戶端 | 商家端 |
|---|---|---|---|---|
| 3-1 | `order` | 用戶下單(步 28) | 訂單成立 | 助手看板 +1 + toast;參謀 toast |
| 3-2 | `status` | 商家按「出餐完成」(步 11) | 訂單跳「請取餐」 | 卡片轉已完成 |
| 3-3 | `coupon` | 商家「發送喚回券」(步 15) | 券夾插入 + toast | 鈕轉已完成 |
| 3-4 | `present` | 用戶「到店出示」(步 26) | toast 已出示 | **核銷台亮 + 自動跳訂單頁**,券名正確 |
| 3-5 | `redeem` | 商家「✅ 確認核銷」 | 該券標**已使用**(浮層開著也即時重繪) | 今日核銷計數 +1、核銷台歸位 |
| 3-6 | `paymode` | 商家開店步 7 切支付 | 切線上付款/到店付 | 開關狀態 |
| 3-7 | `member` | 用戶入會(步 31) | 入會成功 | 助手會員 +1;參謀 toast |
| 3-8 | `queue` | 用戶取號(步 32) | 取號卡 | 助手 toast |
| 3-9 | `booking` | 用戶預約 | 預約卡 | 助手 toast |

---

## 4. 給店家的 3 分鐘小實驗(一鏡到底,一支手機)

只開 `merchant.html`,切分頁演。完整話術見 `../../local-life-lab/docs/04-reference/wechat-merchant-playbook.md`。

1. 增長 → 發送喚回券 →「37 個三週沒來的熟客,一鍵發券」
2. 切用戶端預覽 → 券已在 →「客人手機即時收到」
3. 按「到店出示」→「客人到店給你看這個」
4. 切商家助手 → 核銷台已亮 → 確認核銷 →「**你按一下錢才開始算,沒帶來客人一毛不收**」
5. 指核銷計數 + 參謀券效漏斗 →「每張核銷都對得到發票,月底不用對帳」

**Gate(二選一才算過)**:G1 老闆當場同意掛桌碼物料 / G2 老闆主動問價格抽成。
都沒有 → 記下他卡在第幾步、原話。

---

## 5. 真資料鏈路(不在這個 repo,不影響 demo)

在 `../../miniapps/nest-integration/`:

```bash
python verify-shop.py <shop-UUID>          # 驗店 + 檢查 enableInvoice
python invoice-api.py invoices --carrier /NJRSPN5   # 拉真發票
python carrier-join.py --shop <UUID> --carrier /NJRSPN5 --invoices invoices.json   # 六環對接
```

仍卡:shopId 要到店掃桌碼才拿得到(順帶請老闆開電子發票、下一單真單)。
`INTEGRATION_API_KEY` 只從環境變數讀,不寫檔、不 echo。

## 6. 交付閘門

```bash
python checks/verify-demo.py
```

擋過期日期、跨檔數字打架、統編不一致、「標杆店」回滲、HTML/JS 壞損。
