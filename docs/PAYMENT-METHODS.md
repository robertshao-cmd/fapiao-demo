# 付款方式整合——現況、方法論、兩側各要問誰

> 0803 補。對應 `docs/TEST-STEPS.md` 結帳那一步的註記。
> 這份不是規格,是「拿去問人」的底稿——每一條都寫清楚**問誰、問什麼、為什麼**。

> **0803 更新(Adam 直接回覆,不是猜的):點到本來就有串金流,不是純訂單轉發系統。**
> 點到商家後台「設定」頁有一頁「付款方式設定」——暖暖窩目前**街口支付(JKoPay)已開通**
> (真的 Store ID/Secret/API Key,不是示範值),LINE Pay 選項也在但這家店沒開,
> 信用卡是「即將推出」還沒上。**這改變了下面第三節策略題的答案,見新增的「0803 結論」。**

## 一、現況:demo 裡的付款是模擬,不是真整合

實測過(`user.html` 的 `renderCheckout()`/`isOnline()`):

- **已接第三方支付**(線上):結帳頁三顆鈕(LINE Pay/信用卡/Apple Pay),點下去只是切換 UI 狀態
  (`pick(this)`),**沒有呼叫任何真的金流 SDK 或 API**。下單後直接標記 `paid:true`、生一組假發票號。
- **未接第三方支付**(到店付款):訂單先送出,付款發生在取餐/送達那一刻(現金/店內刷卡/貨到付款),
  外加一個「履約保障」機制——連續跑單會暫停到店付款資格,除非綁信用卡擔保(這段也是模擬)。
- 商家端(`merchant-app.html` 開店步驟 3「營運與支付」)讓商家二選一:接第三方支付(線上收款、
  T+1 撥款)或先用到店付款(0 串接成本);兩種都寫「基礎服務 0 抽成」。
- 核銷台的「3–6%」是**券核銷的抽成**,跟付款處理費是兩件不同的事,不要混在一起問人。

**這一段目前全部是前端狀態切換,沒有任何一行程式碼真的碰過金流。**

## 二、方法論:拆解 quickclick.cc(快一點 LINE點餐)+ DDPay 學到的

(完整拆解見對話紀錄;這裡只留下可以直接套用的三個結論)

1. **單一結帳畫面聚合多種支付方式**——現金/LINE Pay/Apple Pay/Google Pay/信用卡全部長在同一個
   畫面,後面接哪個支付軌是實作細節。**我們的結帳畫面已經是這個形狀,不用改介面,要接的是後面。**
2. **收款跟讀發票載具是同一個物理動作**,不是兩步——他們的收款裝置(商米 V3 MIX)感應收信用卡的
   同時,內建掃碼模組讀客人的發票載具號。這是核銷台未來要往哪個方向長的參考(見 HANDOFF 候選)。
3. **DDPay 是「一次申請開通,多種支付任選」的聚合層**——重點不是他們的裝置,是「商家不用一家一家
   自己申請刷卡機、串 LINE Pay」這個聚合角色本身。**這個角色由誰來扮,是下面第三節要先問清楚的事。**

## 三、先問 Melon 的策略題(比工程問題更前面,順序不能反)

**這題不問清楚,下面工程那幾條問了也白問**——問法向決定了要找誰、要接哪一層。

> 「消費事業群不爭利三原則」的精神下,我們是要:
>
> - **(A) 只當資料層**:付款這件事完全交給點到/商家自己現有的收款方式(可能是點到自己有串,
>   也可能是商家自己的 POS/現金),我們只在事後讀「這筆訂單付了多少、什麼時候付的」——
>   跟現在讀發票 rawdata 的角色一樣,不碰錢的流動本身。
> - **(B) 自己做聚合層**:像 DDPay 那樣,由我們(或找一家像 DDPay 的既有服務商)幫商家把多種
>   支付方式收斂成一個入口,我們就會直接參與金流,要面對 PCI DSS、第三方支付牌照這類合規問題。

demo 目前的介面(結帳頁自己切支付方式)其實已經**假裝**選了 (B),但沒人真的拍板過。
問 Melon 的具體問題:**這條線要不要碰金流本身,還是永遠停在「讀結果」?**

## 0803 結論:現況已經是(A),而且比原本想的更完整

Adam 確認點到自己就有串金流(街口支付/LINE Pay,token 式設定,暖暖窩已開通街口支付)。
這代表:

- **我們完全不用蓋支付聚合層**——這件事點到已經做了,「DDPay 那種聚合角色」點到自己就是。
- 客人在點到線上點餐頁付款,**錢真的當下就走街口支付/LINE Pay 的金流軌**,不是等到店結帳才動——
  這跟原本以「點到不開發票=點到不碰錢」推論出的「現金流向實況」不完全一樣,那份文件要修正
  (見 Google Doc「現金流向實況」章節的 0803 更新)。
- 但**付款≠開發票**還是兩件事:點到有金流不代表點到會開發票(「點到不開發票,發票是店家設備開的」
  這句話還沒被推翻,只是現在多一個問題要確認,見下面新增的問題)。

策略題(A)vs(B)基本上已經有答案——**是(A)**,不用再問 Melon 要不要碰金流本身,
要問的問題**升級成更精確的技術題**,見下一節。

## 0803 追加:實測發現——AILab 平台本來就有街口/LinePay 的代理服務

Adam 給了兩組測試機連結,我實際打開驗證過(不是只看連結,兩邊都是真的能連上的服務):

| 服務 | Base URL | 說明(原文照抄) |
|---|---|---|
| `jko-pay-service` | `https://xlab-test.cmoney.tw/JkoPay` | 「JKOPay OnlinePay 整合服務(stateless proxy)。賣家自帶 X-Jkos-Api-Key / Secret / Store-Id headers;service 不存任何憑證或訂單。」 |
| `line-pay-service` | `https://xlab-test.cmoney.tw/LinePay` | 「LINE Pay v3 沙盒整合服務。無狀態認證——賣家每次請求自帶 X-LinePay-Channel-Id / X-LinePay-Channel-Secret。」 |

兩個都是**無狀態代理**——不存商家憑證,呼叫端(點到,或未來的我們)每次請求自己帶
Store/Channel 的 Api-Key/Secret。這解釋了 Adam 截圖裡點到後台「街口支付設定」欄位
(Store ID/Secret/API Key)是要餵給誰用的——兩者是同一套機制,點到很可能就是呼叫這個代理服務。

關鍵端點:
- `POST /JkoPay/payments`、`GET /JkoPay/payments/{platformOrderId}`(**查付款狀態**)、
  `POST /JkoPay/payments/{platformOrderId}/refund`
- `POST /LinePay/payments`、`GET /LinePay/payments/{transactionId}`、
  `POST /LinePay/payments/{transactionId}/confirm`(LINE Pay 導回後要 confirm 一次)、退款同上

兩邊都附了可以互動測試的 sandbox demo 頁:https://ailab-test.cmoney.tw/JkoPayWeb/ 、
https://ailab-test.cmoney.tw/LinePayWeb/

**demo 頁自己寫的警語(原文照抄,不是我加的)**:
> 「⚠️ Demo only——此頁面把 secret 放瀏覽器,正式 caller 必須從自己後端 server-to-server 呼叫 service。」

**這條警語直接限制我們能不能把 demo 接成真的**:`fapiao-demo` 現在是純前端靜態頁(零後端),
架構上沒有地方能安全存 Secret——要接這個代理服務,不能像現在 `user.html` 的 JS 那樣直接呼叫,
一定要先有一個後端(哪怕是輕量的 serverless function)幫忙擋 Secret,不然就是把商家的
金流密鑰攤在瀏覽器原始碼裡。**這是「demo 結帳按下去變成真的走 sandbox 付款」這件事,
唯一的架構前提,順序要對:先有後端,才能接。**

對 Min 那題的更新:CMoney 內部有沒有既有支付串接團隊/供應商——**答案已經有一半了**:
至少街口/LinePay 這兩軌,AILab/xlab 平台本來就有現成的無狀態代理服務可以掛,不用重新選型;
信用卡目前沒看到對應服務,大概就是點到那邊「即將推出」卡住的原因。

## 四、既然是(A),兩側各要問誰(0803 更新版問題)

| 側 | 要問誰 | 問什麼 |
|---|---|---|
| 商家端 | **Adam / Clark(點到團隊)** | 客人用街口支付/LINE Pay 線上付款成功後,**付款狀態/金額有沒有進到 MR!93 那支唯讀訂單 API**?`GET /JkoPay/payments/{platformOrderId}` 這支既然能查狀態,點到是不是就是這樣查的,那能不能也回傳給我們? |
| 商家端 | **Adam / Clark** | 用街口支付/LINE Pay 線上付款成功的訂單,**發票是不是還要店員手動在自己的設備上另開一次**?還是點到的金流整合已經連動開票?(「點到不開發票」那句話是讀舊版原始碼得出的,金流既然已經整合,這句話可能要更新) |
| 商家端 | **Adam / Clark** | 暖暖窩目前信用卡「即將推出」——上線時程抓多久?這決定 demo 要不要現在就把信用卡選項拿掉,還是保留當「快上了」的展示 |
| 工程 | **Min(Eng GL)** | 若真的要讓 demo 接 sandbox 付款(不是只改文案),我們需要一個能安全存 Secret 的輕量後端——這個由誰起、放哪裡(現有的中台/ailab,還是另開一個),不是我能自己決定的技術選型 |
| 消費者端 | **Min(Eng GL)** | 結帳頁那三顆假鈕(LINE Pay/信用卡/Apple Pay)要改成對應點到真實支援的方式(街口支付+LINE Pay,信用卡標「即將推出」),不要展示假的支付方式清單 |

## 五、(B)已經不太可能發生,留著當歷史紀錄

Adam 確認點到自己有金流整合後,「我們自己做聚合層」這個選項基本上沒有存在的理由——
點到已經扮演了 DDPay 那個角色,重做一次是重複造輪子。下面這節留著,只是记录曾經想過這個選項、
為什麼放棄,不是待辦。

<details>
<summary>(B) 如果答案是要做聚合層——兩側各要問誰(0803 之前的版本,現已不適用)</summary>

| 側 | 要問誰 | 問什麼 |
|---|---|---|
| 兩側都要 | **Min(Eng GL)** | CMoney 內部有沒有既有的支付串接團隊/供應商(類似 DDPay 那種角色),還是要重新選一家?這不是一個人能決定的技術選型,要 GL 層級排 |
| 商家端 | **Adam / Clark** | 點到的訂單模型能不能接受「外部系統回寫付款狀態」(webhook 或 API),還是付款狀態只能由點到自己的收銀端寫入? |
| 消費者端 | **Rebecca(Product GL)** | 發票載具 App 本身有沒有既有的支付能力(App 內建付款、或已跟哪家 PSP 談過),小程序這條線能不能直接借用,不用重新談一次 |
| 法遵 | **Melon** | 一旦碰金流,PCI DSS / 第三方支付服務機構登記這類合規門檻由誰扛、多久能跑完——這會直接影響 demo 能不能在時限前變成真產品 |

</details>

## 六、兩側整合縫在流程圖的哪裡

見 `TEST-STEPS.md` 開頭「消費鏈路全圖」——結帳那一步已經標了註記指回這份文件。
**0803 更新**:縫的位置沒變,但接法確定了——結帳頁三顆假鈕改成對應點到真實支援的方式
(街口支付+LINE Pay 可選,信用卡標「即將推出」),下單後的「已付款」狀態改成讀點到那邊
的真實付款結果(等 MR!93 或新端點回傳付款細節),不用再假裝我們自己在處理金流。
